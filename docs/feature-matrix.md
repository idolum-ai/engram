# Engram Feature Matrix

This table tracks user-facing and operational features. The "tested %" column is
an engineering confidence estimate from current automated and live/manual
coverage, not Go statement coverage.

Readiness levels:

- `ready`: implemented, exercised by `make check`, and usable in the current service.
- `beta`: implemented and usable, but relies meaningfully on live Telegram/tmux/LLM behavior.
- `partial`: implemented with known coverage or behavior gaps.
- `reserved`: intentionally exposed in metadata but not meant for normal use.

| feature_id | description | priority level | related files | tested % | current test types | readiness level |
|---|---|---:|---|---:|---|---|
| `F-001` | Environment loading and validation for Telegram bot token, single allowed user/chat, Haiku-only Anthropic config, workdir, tmux session, and attachment limits. | P0 | `internal/config/config.go`, `.env.example`, `README.md` | 90% | Unit tests for defaults, non-Haiku rejection, malformed numeric values, and weak env file permissions; smoke exercises CLI config path. | ready |
| `F-002` | Local CLI and install flow: `engram run`, `engram commands`, `engram preflight`, `engram status`, `engram dry-start`, `make build`, `make install`, `make install-service`. | P0 | `cmd/engram`, `Makefile`, `README.md`, `requirements/operations.md` | 85% | Build gate, smoke script, local diagnostic tests proving no Telegram/Anthropic/polling calls, live service install/restart during development. | ready |
| `F-003` | Single-process lock keyed by Telegram bot/user/chat settings to prevent duplicate pollers. | P0 | `internal/lockfile`, `internal/app/app.go`, `requirements/security.md` | 90% | Unit test for duplicate live lock, metadata write test, race target covers lockfile package. | ready |
| `F-004` | Telegram authorization: exactly one configured user and one configured chat may mutate state or tmux; rejected callbacks are still answered. | P0 | `internal/app/app.go`, `internal/app/callbacks.go`, `internal/config/config.go`, `requirements/telegram.md`, `requirements/security.md` | 72% | Config tests, unauthorized/stale callback tests, command contract tests, live usage. Needs direct unauthorized-message update tests. | beta |
| `F-005` | Telegram long polling, at-most-once update offset persistence, and bounded update journal. | P0 | `internal/app/app.go`, `internal/state/state.go`, `requirements/reliability.md` | 70% | State persistence and update-journal unit tests; documented crash semantics; live usage. Needs poller retry/crash simulation tests. | beta |
| `F-006` | Command metadata registry powers `/help`, `/commands`, Telegram bot command registration, and local `engram commands`. | P0 | `internal/commands/commands.go`, `internal/app/command_contract_test.go`, `requirements/telegram.md` | 95% | Registry completeness, uniqueness, JSON export, help text, bot-command exclusion, app-command contract tests. | ready |
| `F-007` | Service diagnostics commands: `/status`, `/version`, `/logs`, `/quit`, `/restart`. | P1 | `internal/app/app.go`, `internal/app/files.go`, `internal/commands/commands.go`, `internal/version`, `requirements/operations.md` | 60% | Command metadata/handler contract tests; audit redaction test; log-tail unit test; live service checks. Needs direct command response tests. | beta |
| `F-008` | Compact `/sessions` lists tracked sessions and native tmux panes, with inline watch, confirmed close, and immutable-pane attach controls. | P0 | `internal/app/sessions.go`, `internal/app/callbacks.go`, `internal/telegram/telegram.go`, `internal/tmux/tmux.go`, `requirements/tmux.md` | 82% | Markup tests, close-confirmation test, immutable pane parser tests, live use. Needs app-level sessions text snapshot tests. | beta |
| `F-009` | Default tmux target selection uses configured session, otherwise first existing tmux session, otherwise fallback managed session. | P0 | `internal/app/sessions.go`, `internal/tmux/tmux.go`, `requirements/tmux.md` | 45% | tmux parser tests and live use. Needs direct app-level target-selection tests. | partial |
| `F-010` | Create a new terminal session/window from a top-level message or `/new <text>`. | P0 | `internal/app/sessions.go`, `internal/app/input.go`, `internal/tmux/tmux.go`, `internal/state/state.go` | 55% | tmux send tests, state persistence tests, command contract tests, live use. Needs app-level new-session tests. | beta |
| `F-011` | Attach an existing tmux pane with `/attach <target>` or an immutable `%pane_id` callback; revalidate pane/window identity before mutation. | P0 | `internal/app/sessions.go`, `internal/app/callbacks.go`, `internal/tmux/tmux.go`, `internal/telegram/telegram.go` | 84% | Pane identity/parser tests, mismatch-to-lost app test, attach markup tests, live use. Needs duplicate-attach app test. | beta |
| `F-012` | Outcome-aware session input through anchor replies, `//` slash escaping, `/send`/`/run`, `/text`/`/type`, `/key`, and allowlisted anchor buttons. | P0 | `internal/app/app.go`, `internal/app/input.go`, `internal/app/callbacks.go`, `internal/tmux/tmux.go`, `requirements/tmux.md` | 90% | App-level slash routing, missing-session outcomes, pane validation, literal+Enter, key validation, callback tests, live use. | ready |
| `F-013` | Session management with created/attached provenance: `/rename`, `/cwd`, `/cd`, `/watch`, `/stop`/`/unwatch`, and safe `/close`. | P1 | `internal/app/sessions.go`, `internal/app/callbacks.go`, `internal/app/app.go`, `internal/tmux/tmux.go`, `internal/state/state.go` | 88% | Created-close failure, attached-untrack, close confirmation, identity validation, closed-refresh serialization, metadata, quoting, and live tests. Needs direct rename/cwd response tests. | beta |
| `F-014` | Outcome-focused anchor cards with refresh and compact key buttons, typed edit classification, render hashing, at most four existing paths, and 10s edit cadence. | P0 | `internal/app/refresh.go`, `internal/app/visible_paths.go`, `internal/telegram/telegram.go` | 96% | Formatting, fallback classification, rate-limit/transient-failure no-replacement, deleted replacement, not-modified, terminal-state controls, render stability, path-cap, and live tests. | ready |
| `F-015` | Haiku guide pipeline: visible capture first, repeated-line filtering over the prior five visible captures for visible and full-retry prompts, structured hidden confidence, one optional full-scrollback retry, Telegram-visible status, recommendation, and source-evidence citations. | P0 | `internal/anthropic/anthropic.go`, `internal/app/refresh.go`, `requirements/tmux.md`, `tmux-telegram-client-spec.md` | 84% | Anthropic request/JSON parsing tests, prompt contract tests, deterministic citation eval fixtures, repeated-line filter, full-retry filter, and refresh-reset tests, live use. Needs optional live Haiku evals. | beta |
| `F-016` | Context-bound summary scheduling: immediate tmux input, per-session coalescing, global capture/Haiku caps, adaptive 10-30s polling, and shutdown drain. | P0 | `internal/app/app.go`, `internal/app/refresh.go`, `tmux-telegram-client-spec.md` | 82% | Coalescing, cancellation/drain, global primitive, race, and live tests. Needs scheduler timing integration tests. | beta |
| `F-017` | Purpose-specific capture: semantic visible input for Haiku, physical `/raw`, bounded confidence tail, and streamed physical `/dump`. | P1 | `internal/app/files.go`, `internal/app/refresh.go`, `internal/tmux/tmux.go`, `requirements/tmux.md` | 92% | Exact argv, ANSI cleanup, byte/line and artifact bounds, streaming writer, compatibility, and live tests. Needs app-level document upload tests. | ready |
| `F-018` | Incoming attachments download in bounded background workers, hash during streaming, persist bounded metadata, and list through `/attachments`. | P1 | `internal/app/files.go`, `internal/telegram/telegram.go`, `internal/state/state.go`, `requirements/security.md` | 78% | Stream hash, soft/hard rejection, bounded state, and oversized-download tests. Needs successful end-to-end attachment test. | beta |
| `F-019` | Large attachment soft limit with Telegram-valid `/attachment_bypass sha256:<hash>` and a cloud/disk/config-derived hard limit. | P1 | `internal/app/files.go`, `internal/config/config.go`, `internal/state/state.go`, `requirements/security.md` | 82% | Config, command metadata, hard-limit, streamed soft-limit, one-shot consume/expiry, and hash-stream tests. Needs successful bypass integration test. | beta |
| `F-020` | `/download <absolute-path>` uploads a private snapshot of an opened regular file in a bounded background worker after identity and 50 MiB cloud-limit checks. | P1 | `internal/app/files.go`, `internal/telegram/telegram.go`, `requirements/security.md` | 88% | Symlink/type, path-replacement snapshot, size bound, queue bound, multipart retry, and live-use tests. Needs background upload success/failure integration tests. | beta |
| `F-021` | Audit logging records service, command, Telegram, tmux, state, and Haiku failures; `/logs` exports a recent redacted log tail. | P0 | `internal/app/app.go`, `internal/app/files.go`, `internal/app/refresh.go`, `internal/state/state.go`, `internal/redact/redact.go`, `requirements/reliability.md` | 92% | Audit redaction unit test, log-tail unit test, common secret-shape redaction tests, tracked-file secret scan, live log scrub/restart checks. Needs broader audit event tests. | ready |
| `F-022` | Durable bounded state under `~/.engram`: synced atomic replacement, pruned registries/journals, hashes instead of raw captures, and corrupt recovery. | P0 | `internal/state/state.go`, `requirements/reliability.md` | 96% | Sync/rename behavior, pruning policies, legacy compatibility, corrupt recovery, persistence, and race tests. | ready |
| `F-023` | Typed, token-safe Telegram client with polling, send/edit/callback/document/file primitives, bounded outbound pacing, retry-after, and edit classification. | P0 | `internal/telegram/telegram.go`, `internal/telegram/telegram_test.go` | 96% | Token redaction, transport/API errors, 429 retry, concurrency, multipart retry, download hashing, payload, markup, and formatting tests. | ready |
| `F-024` | tmux primitives for immutable pane inspection/validation, session/window creation, input, semantic/raw/bounded/streamed capture, close, and cwd. | P0 | `internal/tmux/tmux.go`, `internal/tmux/tmux_test.go` | 94% | Exact argv, parser/identity, capture fidelity/bounds/streaming, input, key, target, and quote tests. Needs live tmux integration tests. | ready |
| `F-025` | Markdown/HTML handling for anchors: escape unsafe content and convert small Markdown subset before Telegram HTML send. | P1 | `internal/telegram/telegram.go`, `internal/telegram/telegram_test.go`, `requirements/telegram.md` | 85% | Markdown conversion tests, code-fence tests, HTML send/edit payload tests. | ready |
| `F-026` | Quality gates and repository hygiene: `make check`, architecture boundaries, public readiness, workflow sanity, secret scan, stdlib-only, docs freshness, CI workflows. | P0 | `Makefile`, `scripts`, `.github/workflows`, `internal/architecture/architecture_test.go` | 95% | Local quality gate, architecture test, script checks, CI workflow sanity, tracked-file secret scan, stdlib-only gate, docs freshness gate. | ready |
| `F-027` | Go-stdlib-only dependency posture. | P1 | `go.mod`, `requirements/operations.md`, `THIRD_PARTY_NOTICES.md`, `scripts/check-stdlib-only.sh` | 100% | `go test ./...`, public readiness checks, direct inspection of `go.mod`, `make stdlib-only` gate. | ready |
| `F-028` | Reserved `/kill` command remains registered as reserved metadata but excluded from bot/help output; users should use `/close <id>`. | P2 | `internal/commands/commands.go`, `internal/commands/commands_test.go` | 90% | Bot-command exclusion test and help-text checks. | reserved |
